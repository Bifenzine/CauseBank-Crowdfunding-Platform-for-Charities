

const SectionHeader = ({ title, buttonText, onButtonClick }) => {
    return (
        <div className="flex items-center justify-between mb-6 bg-white p-4 rounded-lg shadow-lg">
            <h4 className="text-2xl font-bold text-black">Support section</h4>

        </div>
    );
};

export default SectionHeader;